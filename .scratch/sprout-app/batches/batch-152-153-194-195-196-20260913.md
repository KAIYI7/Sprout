# Batch batch-152-153-194-195-196-20260913 — coordinator record

Single-writer sequential batch (coordinator implements all five in C:\Sprout; no isolated worker copies — disjoint path/owner claims below, strict sequential integration). Core sync/publish owned by coordinator only.

Baseline: immutable hashes preserved outside synced tree at `C:\Users\admin\AppData\Local\Temp\opencode\batch-152-153-194-195-196-20260913\baseline-hashes.txt` (21 paths, SHA-256 + ABSENT markers). Snapshot taken via `tools\sync.ps1 -Down` (0 copied, 502 files) at session start; `-Up` verified 0 copied before work.

## Ticket table

| Ticket | Dependencies | Baseline | Workspace | Claimed paths/owners | Shared contract / integration owner | Actual changes | Validation assignment/results | State |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 194 exe rename | None — ready | batch-152-153-194-195-196-20260913 | C:\Sprout | `src-tauri/Cargo.toml` ([package] name), `src-tauri/src/worker.rs` (doc + test args), `src-tauri/src/autostart.rs` (test strings), `src-tauri/src/lib.rs` (current_exe error string), `tools/repro-*`, `docs/release/release-process.md` (exe wording); keep `[lib] name=sprout_lib`, `productName=Sprout`, `identifier=com.sprout.app`, `%LOCALAPPDATA%\Sprout`, `SETUP_ASSET_PREFIX=Sprout_`, release.yml glob | 8 files applied from staging (Cargo.toml, lib.rs, worker.rs, autostart.rs, windows_execution/shell.rs, 3 repro scripts); NSIS/release.yml/identifier untouched per template check | cargo check + full backend suite + ownership gate green (combined); no local installer build per releases.md | validated |
| 195 presence assets | 182 payload/seam assumed; portal upload manual prerequisite (cannot verify upload from here — code lands, prerequisite documented) | same | C:\Sprout | `src-tauri/src/presence.rs` (sole Discord IPC owner per ADR-0029), presence shape tests | `.assets(rp_large/Sprout, rp_small/Ready)` + payload test rename + ADR-0033 dated amendment (original + priors untouched) | fake-IPC payload-exactness (backend suite green), ownership gate green | validated |
| 196 animation toggle | None — ready | same | C:\Sprout | `src-tauri/src/settings.rs` (new animation key), `src/lib/settingsSearch.ts`, `src/routes/settings/+page.svelte` (General switch), `src/lib/styles/tokens.css` (data- hook, no new tokens), `ContextMenu`/`Dialog`/`Disclosure`/pulse sites | 16 paths merged (settings.rs, logs.rs fixture, lib.rs update_animation, api.ts updateAnimation, types.ts animation, animation.svelte.ts store, layout + dock window wiring, tokens, 3 components, settings page/search/tests); coordinator SSR repair: document-guarded `apply()` (see ledger) | settings suites + full frontend suite (255) + `npm.cmd run check` 0 errors + ownership gate green | validated |
| 152 stronger model | 151 complete; 146 stronger qualification open (no invented pins — stronger stays non-installable until evidence; implement selection/inventory/migration/validation logic with fixtures) | same | C:\Sprout | `src-tauri/src/ai_managed.rs` (managed seam owner), `src-tauri/resources/ai-model-recommendations.json` (no invented artifact values), Settings AI UI + `src/lib/types.ts`, tests | Catalog validation (duplicate IDs, executable recipes, bad pins, wrong runtime) + orphan inventory (`removed-from-recommendations`, explicit removal) + 7 fixture tests; shipped JSON untouched; Settings UI already adequate | backend suite 642 passed (23 managed incl. 7 new) + frontend suite + ownership gate green | validated |
| 153 diagnosis revisions | 148 implemented awaiting validation (build on checked-draft seam, do not block on later providers) | same | C:\Sprout | `src-tauri/src/ai_assist.rs` (diagnose path, shared rules + diagnose skill), `src-tauri/src/lib.rs` (new diagnose command, no execution), Quick Action dialog revision UI | `request_diagnosis` + `DiagnoseOutcome` + baseline/conflict helpers + `ai_diagnose_draft`/`ai_verify_revision` commands + api/types seam + dialog Diagnose block (accept-only-reviewed, auto-run exposure, save-time recheck) + 6 backend tests | backend suite (35 ai_assist incl. 6 new) + frontend suite + check green; zero Run/Test/Stop calls by construction | validated |

Integration order: 194 (done, validated) → parallel wave: worker-195 (195) + worker-196 (196) + worker-ai (152+153) → coordinator merges in order 195, 196, AI → combined validation → publish. Shared `settings.rs` touched by 196 (animation key) and 152/153 (AI keys already exist — no overlap expected; coordinator owns any merge). No new Windows-invocation sites; ADR-0029 owners extended, never duplicated.

## Parallel wave (dispatched after 194; dev instance stopped, disk freed)

Baseline ID: batch-152-153-194-195-196-20260913 (hashes in Temp batch dir). Workspaces (source-only copies, no target/node_modules/.git/.codegraph/build/.svelte-kit/dist/.sync-state.json):

- worker-195: `...\batch-152-153-194-195-196-20260913\worker-195` — ticket 195 only. Fully disjoint.
- worker-196: `...\batch-152-153-194-195-196-20260913\worker-196` — ticket 196 only. Owns General-group edits.
- worker-ai: `...\batch-152-153-194-195-196-20260913\worker-ai` — tickets 152+153 (AI pair, one owner keeps the AI seam coherent). Owns AI-group edits.
- staging: `...\batch-152-153-194-195-196-20260913\staging` — coordinator reconciliation only.

Shared-file plan (same file, different sections — planned overlap, not a blocker):

- `src/routes/settings/+page.svelte` + `src/lib/settingsSearch.ts`: worker-196 owns the General-group Animation switch; worker-ai owns the AI-group stronger-tier/diagnosis UI. Shared contract: knob-id registry (`SETTINGS_GROUP_KNOBS`) — 196 adds `animation`, AI ids unchanged; no shared components beyond existing primitives. Merge owner: coordinator; order: 196 then AI; combined check: settings suites + `npm.cmd run check` + ownership gate.
- `src-tauri/src/settings.rs`: 196 adds the animation key; worker-ai adds no keys (ai_* exist). No overlap.
- `src-tauri/src/lib.rs`, `src/lib/api.ts`, `src/lib/types.ts`: worker-ai only (new diagnose command + stronger-selection plumbing).

Workers run NO heavy checks (no installs, compilation, app launches in copies); coordinator validates the combination in C:\SProut under the single heavy slot.

## Conflict ledger

- 2026-09-13, `src-tauri/src/lib.rs` + `src/lib/api.ts` + `src/lib/types.ts` (tickets 196 + AI): planned overlap, different sections. 196 adds the animation command/plumbing (`update_animation`, `updateAnimation`, `Settings.animation`); AI adds the diagnose command/plumbing (`ai_diagnose_draft`, `ai_verify_revision`, `AiDiagnoseOutcome`/`AiRevision`). Coordinator merged by hunk (no last-writer-wins): 196 hunks applied onto the AI-built tree, verified by `cargo check`, full backend suite (642), `npm.cmd run check` (0 errors), full frontend suite (255), production build, ownership gate. No semantic conflict: animation and diagnosis share no contract beyond the file.
- 2026-09-13, `src/lib/animation.svelte.ts` (ticket 196, coordinator repair): the store's module-scope `apply(cached)` touched `document` unguarded, crashing the SSR `aiDraftDialog` suite via `Dialog.svelte` (`document is not defined`). Coordinator added a `typeof document === "undefined"` early return in `apply()` (state still updates; attribute write waits for a real document; WHY comment cites ADR-0028). Worker-196's file was otherwise byte-kept. Repro: `npm.cmd run test -- --run src/lib/aiDraftDialog.test.ts` failed before, passes after; full suite 21 files / 255 tests green.
- 2026-09-13, `src/lib/managedSettings.close.test.ts` (pre-existing drift, surfaced by 196 merge): the `getSettings` mock predated `Settings` additions (`animation`, `launch_groups`, `action_groups`, `clip_groups`, `reveal_dwell_ms`, `reveal_sensitivity_px`, `companion_url`, `companion_height_ratio`, `companion_muted`). Coordinator completed both mocks with backend-default values. `npm.cmd run check` 0 errors after.
- 2026-09-13, C:\Sprout stray `src/lib/animation.svelte.ts`: a pre-batch shared-tree copy byte-identical to worker-196's file; kept as 196's file (verified by `fc`), no separate baseline. Recovery snapshots for every applied path live under `Temp\opencode\batch-152-153-194-195-196-20260913\recovery\` (pre-194, pre-194b, pre-195, pre-196).
- Staging mirrors the final tree for all batch paths (194 → 195 → 196 pure → AI combined, in order).

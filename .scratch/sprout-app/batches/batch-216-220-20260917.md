# Batch 216 / 220 — 2026-09-17

**Batch ID:** `batch-216-220-20260917`

**Coordinator working copy:** `C:\Sprout`

**Immutable baseline:** `C:\Users\admin\AppData\Local\Temp\opencode\batch-216-220-20260917\baseline` (pristine copy; `ticket-216`, `ticket-220` and `staging` are identical copies from the same source)

**Baseline manifest:** `C:\Users\admin\AppData\Local\Temp\opencode\batch-216-220-20260917\baseline-hashes.tsv`

**Manifest SHA-256:** `DE7E77A9229357E91A4804BF42F79D889AA55F04D3D6AF7810C43743A69B541C`

The baseline contains 569 source/document files and excludes `.git`,
`.sync-state.json`, `.codegraph`, `node_modules`, `target`, `build`, and
`.svelte-kit`. Planned new-path absence is recorded in
`baseline-absent.txt` beside the manifest (`src\lib\copy\zh-CN.json` — absent).

Core `-Down` ran first in `C:\Sprout` (`0 copied`, snapshot 562 files), then
`docs/adr/README.md` plus ADRs 0020/0028/0029 were read before planning.
Topic reads: `docs/agents/parallel-tickets.md`, `docs/agents/structure.md`,
`docs/agents/worker.md`, `docs/agents/conventions.md`, `docs/agents/toolchain.md`,
`docs/agents/ui-ux.md`, `docs/agents/comments.md`.

Dependencies: 215 applied+validated 2026-09-17 (bezel geometry contract);
219 applied+validated 2026-09-16, revalidated 2026-09-17 (EN keys frozen).
216 blocked by 215 → ready. 220 blocked by 219 → ready. 216 and 220 are
independent of each other (wave 1, both dispatch now).

## Plan and state

| Ticket | Dependencies | Baseline / workspace | Claimed paths and owner symbols | Shared contract / integration owner | Validation assignment | Actual changes | State |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 216 Bezel Y-position store + drag + per-monitor restore | 215 (store keys + geometry contract) — met | baseline above / worker `...\batch-216-220-20260917\ticket-216` | `src-tauri/src/db.rs` (`KEY_BEZEL_Y_RATIO_PREFIX`, `save_bezel_y_ratio`, `load_bezel_y_ratio`, `load_bezel_y_ratio_identified` — extends `db` dock-memory owner); `src-tauri/src/quick_window.rs` (`memory_key`, `monitor_refs`, `resolve_bezel_y_ratio` + `dock()` restore hook); `src-tauri/src/lib.rs` (`get_display_bezel_y_ratio` + `set_display_bezel_y_ratio` + registration, reuses `per_display_key`/`resolve_display_keys`); `src-tauri/src/settings.rs` (`validate_bezel_y_ratio` only if following width/companion precedent — else reuse `clamp_bezel_y_ratio` as truth, no new knob); `src/lib/api.ts` (bezel-Y command wrappers, no JS constants); `src/routes/quick-launch-window/+page.svelte` (docked bezel-tab vertical drag + keyboard nudge, live move + persist on release, clamp + center fallback); `docs/adr/0020-monitor-identity-seam-eligibility.md` (dated amendment only); `docs/CONTEXT.md` (bezel-Y-position glossary line with `planned` + spec-214 link); ticket 216 ACs | Owns `bezel_y_ratio` key under identity-key pattern (ADR-0020 amendment), clamp 0..1 on load, center (0.5) fallback on disconnect/resize, side reuses existing edge memory (no new side store). Geometry constants NOT touched (215 owns: collapsed 14px / peek 24px / height 12% clamped 64–160 / open cap 60% overlay / zero reservation). Frontend reads dimensions via `get_bezel_*` commands, never JS constants. | Worker implements in `ticket-216`; owns Rust tests (save/restore/clamp/fallback incl. identity-preferred-over-device-name) + frontend drag/clamp tests where cheap. Lightweight checks only locally (no installs/builds/watchers). Heavy checks queued for coordinator slot in `C:\Sprout`: `cargo test`, `npm.cmd run check`, `node tools/ownership-gate.mjs`. | 10 paths (9M+1A: +`src/lib/bezelYPosition.test.ts`); r2 repairs: stray `}` in quick_window.rs, missing `KEY_BEZEL_Y_RATIO_PREFIX` const + wrong test identity in db.rs | validated |
| 220 zh-CN scaffold + fallback + translation (after EN) | 219 (EN keys frozen) — met | baseline above / worker `...\batch-216-220-20260917\ticket-220` | New `src/lib/copy/zh-CN.json` (exact 219 key set, no new keys); `src/lib/copy/index.ts` (locale-aware resolve with key-by-key EN fallback — extend loader, never duplicate); `src/lib/copy/copy.test.ts` (coverage: every EN key present in zh-CN, missing-key fallback, no blank renders); `src/routes/settings/+page.svelte` (language switch surface only — persisted preference machine-local like theme, one stated live-vs-restart behavior in the hint); `src/lib/api.ts`/`src/lib/types.ts` + `src-tauri/src/settings.rs` + `src-tauri/src/lib.rs` + `src-tauri/src/backup.rs` ONLY as needed for the persisted preference (investigate theme precedent `save_theme`/`update_theme` first; files-only copy, never DB-backed copy store); ticket 220 ACs | Consumes frozen 219 EN key set (`section.key`, 140-char cap, noun titles + Discord-tone descriptions per ADR-0028 2026-09-17 amendment, ticket-33 bans). zh-CN adds no keys of its own. Loader fallback proof required. Preference travels like other Settings; whole-app backup unchanged in shape. No ADR text changes; no CONTEXT changes. | Worker owns loader/coverage tests; runs only lightweight checks locally (`npm.cmd test -- <copy test>`, narrow `tsc`-class checks — no installs/builds/watchers). Heavy `npm.cmd run check` + gate queued for coordinator slot. Full translation reviewed against voice rules (natural, plain, no MT artifacts). | 13 paths (11M+2A: +zh-CN.json, +locale.svelte.ts) + r2 repair to src/lib/settingsSearch.test.ts (SNAPSHOT language fields + surface test); approved deviations: +2 EN keys (settings.language.*, switch surface only), pipe pinned to englishCopy (header translates, Discord stays English — no ADR change), settingsSearch.ts knob entry, locale.svelte.ts beside dictionary | validated |

## Integration order

1. Either return may reconcile first — expected disjoint except shared-hotspot
   command/seam files below.
2. Reconcile each return into `...\batch-216-220-20260917\staging` by applying only changed paths (three-way baseline/worker/staging where needed).
3. Planned shared-hotspot overlap (same file / different symbols — integration,
   not a behavioral dependency): `src-tauri/src/lib.rs` (216 bezel-Y commands
   vs 220 language commands), `src/lib/api.ts` (216 bezel-Y wrappers vs 220
   language wrappers), `src-tauri/src/settings.rs` (216 `validate_bezel_y_ratio`
   vs 220 language preference), `src-tauri/src/backup.rs` (220 only if the
   language-preference investigation requires it). Coordinator owns all merges
   in staging; no silent last-writer-wins. `docs/CONTEXT.md` is 216-only;
   `src/lib/copy/*` is 220-only; `constants/window.rs` is touched by neither
   (215 owns it).
4. Apply one reviewed candidate to `C:\Sprout` with pre-apply recovery, validate the combination, mark ACs only for validated tickets, run the ownership gate, publish with guarded `-Up`, verify second `-Up` is `0 copied`.

## Shared contracts (pinned before consumers start)

- Bezel geometry (215, frozen): collapsed 14px / peek 24px / height 12% clamped
  64–160px / open cap 60% overlay / zero reservation collapsed+peek / centered Y
  default 0.5 (`BEZEL_Y_RATIO_DEFAULT`, `clamp_bezel_y_ratio`, `bezel_tab_y_px`
  in `constants/window.rs` — 216 reuses, never redefines).
- Bezel Y memory: `bezel_y_ratio 0..1` saved beside existing dock memory under
  the identity-key pattern (EDID preferred, device-name fallback — same
  `memory_key`/`per_display_key` seam as `save_dock_edge/mode/width_pct`);
  clamp on load to visible edge bounds at current resolution; disconnect/resize
  falls back to vertical center — never off-screen. Side reuses edge memory.
- Copy keys: `section.key` (219 frozen EN set). EN is the fallback source;
  zh-CN ships the identical key set with translated values, no new keys.
  Same exact text reuses the key (V1 scope only). Voice: noun titles +
  Discord-tone descriptions per ADR-0028 2026-09-17 amendment (spec-214
  2nd-person rule superseded); 140-char hint cap + ticket-33 bans hold.
- Presence: ADR-0033 allowlist unchanged; wording-only (219 already wired).
- No second Windows-invocation site: 216 extends `db`/`quick_window` dock-memory
  owner only. 220 adds no native calls. Gate must pass unchanged in shape.

## Candidate and recovery ledger

- Baseline manifest SHA-256: `DE7E77A9229357E91A4804BF42F79D889AA55F04D3D6AF7810C43743A69B541C` (569 files).
- Workspaces: worker `ticket-216`, worker `ticket-220`, reconciliation `staging`, all under `C:\Users\admin\AppData\Local\Temp\opencode\batch-216-220-20260917\`.
- Coordinator runs no ticket — both tickets in isolated workers; coordinator reconciles, validates, and publishes.
- Returns: `ticket-216` r1 + `ticket-220` r1 received (frozen workspaces kept); repairs `ticket-220-r2` (settingsSearch.test.ts SNAPSHOT) + `ticket-216-r2` (stray brace, missing key const, test identity) received and reconciled.
- Candidate `candidate-216-220-r1` (20 files, manifest SHA-256 `6301AAF90EA96024D53D805A5F809CD766D195A6E91C4E9660D3245C204B6FD3`) applied to `C:\Sprout` 2026-09-17; pre-apply recovery at `recovery/before-apply` (20 files incl. recorded absences for the 3 new files). Follow-up file `src/lib/settingsSearch.test.ts` (r2) + r2 `db.rs`/`quick_window.rs` applied on top with the same recovery dir extended.
- Validation on the applied combination: `npm.cmd run check` 0 errors (3 pre-existing warnings elsewhere — QuickActionFormDialog ×2, UnifiedHeader banner ×1, same as batch 215/219); affected vitest files 69/69 pass (copy incl. 8 new zh tests, settingsSearch incl. new language test, bezel contract, windowChrome); `cargo test` 711 passed, 0 failed, 3 ignored (706 baseline + 5 new bezel-Y: 2 db + 1 settings + 2 quick_window); `node tools/ownership-gate.mjs` pass (62 owned refs, final tree).
- Full `npm.cmd test`: 396/397 pass — the single failure (`managedSettings.close.test.ts` expects "Available to install") is PRE-EXISTING 219 fallout, not this batch: the string exists nowhere in the batch baseline `en.json` (219 renamed it to "Available models"); staging carries 219's value unchanged and neither ticket touches AI copy. Left for its owner; blocks nothing here.
- 216: validated. 220: validated.

## Conflict ledger

None at dispatch. Planned shared-hotspot files (`src-tauri/src/lib.rs`,
`src/lib/api.ts`, `src-tauri/src/settings.rs`) have named symbols/intent above
with coordinator-owned merge. If either return touches outside its claim (esp.
`constants/window.rs`, ADRs beyond 0020, or `docs/CONTEXT.md` beyond the
bezel-Y line), stop and reconcile before staging.

## Conflict ledger — integration 2026-09-17

No silent last-writer-wins. All three overlapping files were pure insertions
against baseline (verified: baseline is a subsequence of each worker version),
so the coordinator merged by interleaving: `lib.rs` = 216 bezel-Y commands +
220 language commands (separate fns + separate registration lines);
`settings.rs` = 216 `validate_bezel_y_ratio` + 220 `KEY_LANGUAGE` /
`DEFAULT_LANGUAGE` / `validate_language` / `load_language` / `save_language`;
`api.ts` = 216 bezel-Y wrappers + 220 language wrappers. Each merged file
verified to contain both symbol sets. Approved deviations recorded on ticket
220's row (switch-surface keys, English-pinned Discord pipe, search-knob entry,
`locale.svelte.ts` placement). The `managedSettings.close.test.ts` full-suite
failure is pre-existing 219 fallout (see validation above), not a batch
conflict. `qual-7b/` temp from the prior batch left untouched.

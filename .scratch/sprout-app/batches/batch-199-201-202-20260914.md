# Batch batch-199-201-202-20260914 — coordinator record

Coordinator owns Core sync/publish + integration + heavy validation. Two ticket workers (199, 201) run in isolated copies; coordinator implements 202 concurrently in its own copy. No shared-tree writing.

Baseline: immutable hashes at `C:\Users\admin\AppData\Local\Temp\opencode\batch-199-201-202-20260914\baseline-hashes.txt` (525 files, SHA-256, sorted `HASH  relpath`). Snapshot via `tools\sync.ps1 -Down` (0 copied, 525 files) 2026-09-14. Baseline ID: `batch-199-201-202-20260914`.
Parent spec: `.scratch/sprout-app/issues/198-prerequisite-offload-motion-spec.md`. ADRs read: 0029 (ownership), 0032 (skills/detection), 0034 (motion tokens); plus 0028/0030/0031/0019 per spec-198 context.

## Ticket table

| Ticket | Dependencies | Baseline | Workspace | Claimed paths/owners | Shared contract / integration owner | Actual changes | Validation assignment/results | State |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 199 prereq detect backend | None — ready wave 1; supplies 200/203 | batch-199-201-202-20260914 | `...\batch-199-201-202-20260914\worker-199` | `src-tauri/src/windows_execution/` (process.rs timed owner + windows_execution.rs re-export seam); `src-tauri/src/winget/` + `winget/authoring.rs` as prior art only; `src-tauri/src/lib.rs` new detect command; `src/lib/api.ts` + `src/lib/types.ts` seam additions; Rust tests | Settles verdict shape (`present` / `not-found for X` / `not-verifiable` + version semantics), timebox value, no-execution proof; contract documented for 200. Integration owner: coordinator | RETURNED FROZEN: new `windows_execution/prereqs.rs` (probe→verdict seam, `detect_with`/`needs_winget_snapshot`/`NativeProbes`/`PrerequisiteVerdict`, 15s/probe + 60s budget, 10 tests); `windows_execution.rs` +1 mod + narrowed re-export (coordinator repair); `lib.rs` +`detect_prerequisites` async cmd + handler; `api.ts` +`detectPrerequisites`; `types.ts` +`PrerequisiteStatus`/`PrerequisiteVerdict` | Worker: re-reads + forbidden-symbol grep green; heavy queued. Coordinator: cargo check green (after warning repairs); cargo test running | returned |
| 201 offload wave 1 | None — ready wave 1; supplies 203 | batch-199-201-202-20260914 | `...\batch-199-201-202-20260914\worker-201` | `src-tauri/src/plan.rs` + `lib.rs::compute_plan`; backup `inspect`/`import` (`src-tauri/src/backup.rs`, `import_export.rs` to confirm); `src-tauri/src/logs.rs` list/tail; `src/lib/settingsSearch.ts` index build + settings page wiring; `spawn_blocking` executor pattern | `async` + `spawn_blocking`, compatible Tauri signatures, short SQLite locks, cancellation/timeout preserved; `tools/repro-tab-freeze.mjs` budget gates. Integration owner: coordinator | RETURNED FROZEN: `lib.rs` 5 conversions (`inspect_backup`, `import_backup`, `compute_plan`, `read_run_progress`, `list_logs`) to async+`spawn_blocking`; `backup.rs` split into `parse_backup_document`/`merge_backup_document` (old `import_backup` kept as `#[cfg(test)]` composition by coordinator repair). COORDINATOR ADDITION in staging: `quick_install_plan` co-converted (same heavy shape). Deviation ACCEPTED with evidence: settings-search index stays frontend (memoized `$derived`, ~30 entries, not rebuilt per keystroke). | Worker: no heavy runs (policy); repro + suites queued. Coordinator: cargo check green; cargo test running; repro queued | returned |
| 202 motion tokens | ADR-0034 + 196 off-switch assumed; ready wave 1; supplies 203 | batch-199-201-202-20260914 | `...\batch-199-201-202-20260914\coord-202` (coordinator-owned) | `src/lib/styles/tokens.css` (add `--dur-slow:280ms` + `--ease-spring`, keep `--ease-out`/`--dur-fast`/`--dur`); `Dialog.svelte`, `Disclosure.svelte` chevron, accordion surface, inputs, `ContextMenu.svelte` (`menu-in` values kept), `PacketCard.svelte` (cap 300ms); `src/lib/animation.svelte.ts` off-switch extension | Token-only values, asymmetric enter/exit, transform/opacity (+border-color/color inputs) only, 196-accordion supersede note. Integration owner: coordinator | IMPLEMENTED by coordinator: tokens.css +2 tokens + reduced-motion delay zeroing; Dialog in 200/cubicOut + out 100/cubicIn (was single 140ms fade); GroupAccordion `fly` 200ms opacity/transform (was instant) with off-path; PacketCard entrance 360→300ms + `--ease-spring`; 7 input/textarea sites border-color-only; chevron + `menu-in` verified already-exact, untouched | Coordinator: CSS/Svelte token+property audit green; npm check + frontend tests queued | awaiting-validation |

Integration order: returns integrate in arrival order (disjoint — no ordering constraint); combined validation before each `-Up`. Staging: `...\batch-199-201-202-20260914\staging` (reconciliation only). Recovery snapshots per candidate under `...\batch-199-201-202-20260914\recovery\<candidate-id>\` (pre-apply contents + hashes, incl. ABSENT).

Resource policy: one coordinator-controlled heavy slot in `C:\Sprout` (existing caches). Workers run NO heavy checks (no installs, compilation, app launches, dev servers). Coordinator validates combinations under steps 8–9.

Overlap resolution: file overlap alone is not a behavioral dependency. 199 vs 201 both touch `src-tauri/src/lib.rs` + `src/lib/api.ts` + `src/lib/types.ts` but different commands/sections (detect vs async conversions) — planned compatible overlap, three-way merge in staging, no last-writer-wins. 202 is frontend-only, fully disjoint from both. No new Windows-invocation sites: 199 extends `windows_execution/` owner; 201 converts executors without new invocation sites; 202 touches no Rust. No duplicate owners.

## Conflict ledger

- 2026-09-14, `src-tauri/src/lib.rs` (tickets 199 + 201): planned compatible overlap, disjoint sections. 199 inserts `detect_prerequisites` + handler entry (~line 2600/3650 region); 201 converts 5 commands to async (~lines 286–680). Coordinator merged by hunk in staging (201 file as base + 199 insertions verbatim), verified by bidirectional `fc`: staging↔199 shows only 201 hunks, staging↔201 shows only 199 hunks. No last-writer-wins. COORDINATOR ADDITION: `quick_install_plan` co-converted in staging (same heavy shape as `compute_plan`, already short lock) — worker-201 flagged it as out-of-allow-list; recorded in 201's ticket.
- 2026-09-14, zero-warning repairs (coordinator, in `C:\Sprout` post-apply): (a) `windows_execution.rs` re-export narrowed to actually-used names (199 over-exported 4 names); (b) `backup.rs::import_backup` gated `#[cfg(test)]` (201's split orphaned it for the lib target; 40 test sites keep working); (c) `prereqs.rs::DETECT_PROBE_ALLOW_LIST` gated `#[cfg(test)]` (test-only oracle). cargo check now shows only pre-existing `files.rs::quote_staged_path` dead-code warning — that file is byte-identical to baseline and outside every allow-list; left for ticket 137 (zero-warnings owner).
- 2026-09-14, 201 deviation adjudication: settings-search index build NOT moved to backend — ACCEPTED with evidence (`buildSettingsSearchIndex` runs in a memoized `$derived` over ~30 entries, rebuilt on settings change, never per keystroke; per-keystroke work is the trivial string matcher). A backend command would add IPC for sub-ms work. Recorded in 201's ticket as verified-already-trivial.
- 2026-09-14, 199 deviation adjudication: no separate `cancel_detect` command — ACCEPTED (per-probe process-tree kill at 15s box + 60s total budget + async abandon = bounded cancellation; `cancelled` flag plumbed for 200 if dialog UX needs it). `winget::snapshot()` unbounded-call exposure noted as pre-existing (same as `compute_plan`), flagged for 203.
- 2026-09-14, 202 audit: all CSS durations/easings in the six surfaces reference tokens except ticket-prescribed `rise 300ms` cap and Svelte numeric params (Dialog 200/100, accordion 200 — token-mirroring with ADR-0034 comments). No `transition: all`, no layout-property animation. PacketCard hover (`transform`+`border-color`) and dots-button (`opacity`+`background`+`color`) paint transitions retained as token-based pre-existing design; inputs reduced to border-color-only per AC.

Candidate: `candidate-1` (199 + 201 + 202 + coordinator `quick_install_plan`), applied to `C:\Sprout` post-baseline-verification (16/16 destinations matched, 1 ABSENT-OK). Recovery snapshot: `...\batch-199-201-202-20260914\recovery\candidate-1\` (pre-apply contents + ABSENT marker). Staging mirrors the candidate except the 3 post-apply warning repairs (backup `#[cfg(test)]`, re-export narrowing, allow-list `#[cfg(test)]`), which live in `C:\Sprout` only until re-synced to staging after validation.

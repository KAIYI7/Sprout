# Batch 215 / 219 — 2026-09-16

**Batch ID:** `batch-215-219-20260916`

**Coordinator working copy:** `C:\Sprout`

**Immutable baseline:** `C:\Users\admin\AppData\Local\Temp\opencode\batch-215-219-20260916\ticket-215` (pristine copy; `ticket-219` and `staging` are identical copies from the same source)

**Baseline manifest:** `C:\Users\admin\AppData\Local\Temp\opencode\batch-215-219-20260916\baseline-hashes.tsv`

**Manifest SHA-256:** `6709D34F4E7DD98F24B6FC71611481E354751060AD7C043EFE299B5B8CCA761D`

The baseline contains 564 source/document files and excludes `.git`,
`.sync-state.json`, `.codegraph`, `node_modules`, `target`, `build`, and
`.svelte-kit`. Planned new-path absence is recorded in
`baseline-absent.txt` beside the manifest (`src/lib/copy/en.json`,
`src/lib/copy/index.ts`, `src/lib/copy/copy.test.ts` — all absent).

Core `-Down` ran first in `C:\Sprout` (`0 copied`, snapshot 557 files), then
`docs/adr/README.md` plus ADRs 0011/0019/0021/0033 were read before planning.

## Plan and state

| Ticket | Dependencies | Baseline / workspace | Claimed paths and owner symbols | Shared contract / integration owner | Validation assignment | Actual changes | State |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 215 Bezel mode backend | None — first mover, defines geometry contract | baseline above / worker `...\batch-215-219-20260916\ticket-215` | `src-tauri/src/constants/window.rs` (bezel constants + pure helpers, single size source); `src-tauri/src/appbar.rs` (bezel rect composition + zero reservation, extends existing dock/AppBar owner); `src-tauri/src/quick_window.rs` (`dock`, `reposition`, `apply_dock_mode`, `set_dock_mode`, `settle_mode`, `autohide_tick`, `drift_check`, `needs_reestablish`, `apply_width` bezel branches); `src-tauri/src/settings.rs` (`validate_dock_mode` accepts `bezel`); `src-tauri/src/lib.rs` (new `get_bezel_*` commands + registration); `src/lib/api.ts` (bezel command wrappers, no JS constants); `docs/adr/0011*`, `0019*`, `0021*` (dated amendments only); `docs/CONTEXT.md` (bezel-tab glossary line only); ticket 215 ACs | Owns bezel geometry contract: collapsed 14px / peek 24px / height 12% clamped 64–160px / open cap 60% overlay / zero reservation collapsed+peek / centered Y default 0.5 (216 owns drag/persist). Pins `dock.bezel.tooltip` key name for 219 (backend never holds UI strings). `docs/CONTEXT.md` bezel-tab line merges with 219's copy-dictionary line in staging (coordinator owns merge). | Worker implements in `ticket-215`; owns Rust tests for new geometry. Heavy checks queued for coordinator slot in `C:\Sprout`: `cargo test`, `npm.cmd run check`, `node tools/ownership-gate.mjs`. No heavy work in the isolated copy by default. | 11 paths applied via candidate-215-219-r1 + 8 coordinator comment repairs; check/tests-compile/gate green, `cargo test` 706 passed 2026-09-17 (freed incremental cache only; build outputs kept) | validated |
| 219 Copy dictionary | None — content work; bezel key names pinned in spec 214 | baseline above / worker `...\batch-215-219-20260916\ticket-219` | New `src/lib/copy/en.json` (`section.key`), new `src/lib/copy/index.ts` loader with EN fallback (+ voice-rules doc header), new `src/lib/copy/copy.test.ts` (fallback + key coverage); `src/routes/settings/+page.svelte` (knob labels/hints via dictionary only); `src/routes/quick-launch-window/+page.svelte` (dock tooltips via dictionary, incl. new `dock.bezel.*`); `src/lib/windowChrome.ts` + `src/routes/+layout.svelte` (presence details/state via dictionary, ADR-0033 allowlist unchanged wording-only); `docs/CONTEXT.md` (copy-dictionary glossary line only); ticket 219 ACs | Consumes pinned `dock.bezel.*` names (provides `dock.bezel.tooltip` + peek hint for future 217; no behavior). Voice rules: 2nd person, verb-led, front-loaded, ≤140 chars/hint, no passive robot voice, no fancy words; ticket 33 bans hold. Errors/onboarding/empty states untouched; files only, nothing in DB. `docs/CONTEXT.md` line merges with 215's in staging. | Worker owns loader/coverage tests; runs only lightweight checks locally (`npm.cmd test -- <copy test>`, `npx tsc --noEmit`-class narrow checks if needed — no installs/builds/watchers). Heavy `npm.cmd run check` + gate queued for coordinator slot. | 9 paths applied via candidate-215-219-r1; check 0 errors, copy tests 16/16, gate pass | validated |

## Integration order

1. Either return may reconcile first — paths are disjoint except `docs/CONTEXT.md`.
2. Reconcile each return into `...\batch-215-219-20260916\staging` by applying only changed paths (three-way baseline/worker/staging where needed).
3. `docs/CONTEXT.md` is the one planned overlap: keep both glossary lines (bezel tab + copy dictionary, each with `planned` qualifier + spec-214 link). Coordinator owns this merge; no silent last-writer-wins.
4. Apply one reviewed candidate to `C:\Sprout` with pre-apply recovery, validate the combination, mark ACs only for validated tickets, run the ownership gate, publish with guarded `-Up`, verify second `-Up` is `0 copied`.

## Shared contracts (pinned before consumers start)

- Bezel geometry: collapsed visible width 14px, peek width 24px, height = 12% of docked monitor height clamped to 64–160px physical, Y default centered (ratio 0.5, clamped 0..1; 216 owns persist/drag). Open = full dock width at resolved width-% with 60% overlay cap (auto-hide-like, reserves nothing extra). Collapsed + peek reserve zero workspace via the auto-hide zero-width `reserve`/`ABM_SETPOS` pattern; open overlays like auto-hide.
- Frontend never hard-codes bezel numbers: Svelte reads them via new `get_bezel_*` Tauri commands (`src/lib/api.ts` wrappers). Constants live only in `constants/window.rs`.
- Copy keys: `section.key` (e.g. `settings.theme.label`, `dock.bezel.tooltip`, `presence.details`, `presence.state.*`). EN is the fallback source; `zh-CN` arrives in 220 and must not gain keys of its own. Same exact text reuses the key (dedup in V1 scope only).
- Presence: ADR-0033 allowlist unchanged; wording-only. Backend `presence.rs` `DETAILS`/`STATE`/validation stay the source of truth for the pipe; frontend dictionary supplies the same fixed vocabulary to `setPresenceStatus`.
- No second Windows-invocation site: 215 extends `quick_window`/`appbar` owners only. 219 adds no native calls. Gate must pass unchanged in shape.

## Candidate and recovery ledger

- Baseline manifest SHA-256: `6709D34F4E7DD98F24B6FC71611481E354751060AD7C043EFE299B5B8CCA761D` (564 files).
- Workspaces: worker `ticket-215`, worker `ticket-219`, reconciliation `staging`, all under `C:\Users\admin\AppData\Local\Temp\opencode\batch-215-219-20260916\`.
- Coordinator runs no ticket — both tickets in isolated workers; coordinator reconciles, validates, and publishes.
- Returns: `ticket-215` r1 + `ticket-219` r1 received (handoff messages; frozen workspaces kept; `returns/` dir removed in cleanup).
- Candidate `candidate-215-219-r1` (19 files, manifest SHA-256 `D8042B067F94DFE32454871555FC5B81696A348C2BB4C7B0C19D4644326088CC`) applied to `C:\Sprout` 2026-09-16; pre-apply recovery at `recovery/before-apply` (16 files + 3 recorded absences). Coordinator repairs on top: 8 comment fixes (no ticket-number refs per comments.md; `#[allow(dead_code)]` + WHY for `bezel_peek_rect`, used by tests only so far).
- Validation on the applied combination: `npm.cmd run check` 0 errors (3 pre-existing warnings elsewhere); `npm.cmd test -- src/lib/copy/copy.test.ts` 16/16 pass; `cargo check --tests` green (no new warnings); `node tools/ownership-gate.mjs` pass (62 owned refs). `cargo test` EXECUTION BLOCKED — C: 2.7 GB free, test-harness link fails os error 112; rerun after freeing disk (target/ is 12.8 GB; kept per keep-build instruction).
- 219: validated. 215: validated 2026-09-17 (`cargo test` 706 passed, 0 failed, 3 ignored — incl. all 9 new/extended bezel tests).

## Conflict ledger

None at dispatch. Only planned overlap is `docs/CONTEXT.md` (two glossary lines); coordinator merges both. Settings-page file overlap with 218 is out of batch (218 not started). If either return touches outside its claim, stop and reconcile before staging.

## Conflict ledger — integration 2026-09-16

No conflicts. `docs/CONTEXT.md` merged cleanly: 215 inserts the Bezel-tab block after Quick Launch dock (line 101), 219 appends the Copy-dictionary block at end — merged file is byte-identical to baseline plus exactly those two additions (verified by diff). No other path overlaps; neither return touched outside its claim. `qual-7b/` temp left in place (live process holds its files).

# Batch 217 + 216fix/220fix — 2026-09-17

**Batch ID:** `batch-217-216fix-220fix-20260917`

**Coordinator working copy:** `C:\Sprout`

**Immutable baseline:** `C:\Users\admin\AppData\Local\Temp\opencode\batch-217-216fix-220fix-20260917\baseline` (pristine copy; `ticket-217`, `ticket-i18n` and `staging` are identical copies from the same source)

**Baseline manifest:** `C:\Users\admin\AppData\Local\Temp\opencode\batch-217-216fix-220fix-20260917\baseline-hashes.tsv`

**Manifest SHA-256:** `A5E2716C33E56B9ABAA14B1C0D2566998641744B725DE9D51B3430F5B52C8C05`

The baseline contains 573 source/document files and excludes `.git`,
`.sync-state.json`, `.codegraph`, `node_modules`, `target`, `build`, and
`.svelte-kit`.

Core `-Down` ran first in `C:\Sprout`, then `docs/adr/README.md` plus ADRs
0011/0019/0020/0021/0028/0029/0033/0034 were read before planning.
Topic reads: `docs/agents/parallel-tickets.md`, `docs/agents/structure.md`,
`docs/agents/worker.md`, `docs/agents/planning.md`, `docs/agents/conventions.md`,
`docs/agents/toolchain.md`, `docs/agents/ui-ux.md`, `docs/agents/comments.md`.
Research: `docs/research/0027-discord-settings-copy-tone.md`, `docs/research/0006-notion-design-patterns.md`.

Dependencies: 215 applied+validated (bezel geometry contract); 219 applied+validated
(EN keys frozen, 81 keys); 216 applied+validated (bezel Y store); 220 applied+validated
(loader + zh-CN 81/81). 217 blocked by 215 → ready. i18n-full (216fix+220fix) blocked by
219 → ready. 217 and i18n-full are behaviorally independent except the planned
same-file overlap below (integration, not a dependency) — wave 1, both dispatch now.

## Plan and state

| Ticket | Dependencies | Baseline / workspace | Claimed paths and owner symbols | Shared contract / integration owner | Validation assignment | Actual changes | State |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 217 Bezel peek/open/close interaction + motion + discoverability | 215 (geometry + open/close hooks) — met | baseline above / worker `...\batch-217-216fix-220fix-20260917\ticket-217` | `src/routes/quick-launch-window/+page.svelte` (bezel tab DOM + hover-peek/class, click toggle, click-outside + Esc close, focus-loss stays open, focusable tab + aria-expanded, tooltip via `t("dock.bezel.tooltip")` + peek hint via `t("dock.bezel.peekHint")`, one-time pulse/glow dismissed permanently); `src/lib/styles/tokens.css` consumers only (reuse `--dur-slow:280ms` + `--ease-spring`, ADR-0034 untouched, no new tokens); new/updated interaction tests (hover-never-opens, toggle, outside/Esc close, focus-loss stays open); ticket 217 ACs | Consumes frozen 215 geometry via existing `get_bezel_*` commands (collapsed 14px / peek 24px / height 12% clamped 64–160 / open cap 60% overlay / zero reservation); consumes frozen 219 keys (`dock.bezel.tooltip`, `dock.bezel.peekHint`) via `t()` — MUST NOT edit `src/lib/copy/*.json` (i18n owns dictionary; 217 returns requested key text if a pulse key is genuinely needed, coordinator routes it to i18n). Respects ADR-0019 (no driver fork — placement outside driver, driver pass returns immediately for bezel). No ADR text changes. | Worker implements in `ticket-217`; owns interaction tests + lightweight checks only (no installs/builds/watchers). Heavy checks queued for coordinator slot in `C:\Sprout`: `npm.cmd run check`, relevant vitest, `node tools/ownership-gate.mjs`. | 2 code paths (M quick-launch page + A src/lib/bezelInteraction.test.ts); r1 frozen; no dictionary touch; one new error literal flagged for i18n follow-up | validated |
| i18n-full Full-app copy coverage + zh-CN consistency (fixes 216+220 gaps: menu, button, placeholder, dropdown, page titles, descriptions) | 219 (EN keys frozen) — met, extends V1 scope (81 keys) to full app | baseline above / worker `...\batch-217-216fix-220fix-20260917\ticket-i18n` | Owns `src/lib/copy/en.json` (add keys `section.key` for every uncovered surface: nav/page titles, buttons, menus incl. ContextMenu/Select/DockVisibilityFilter/PageFeaturesButton, placeholders, dropdown labels, descriptions/hints, dialog copy in scope), `src/lib/copy/zh-CN.json` (identical key set, full translations, `{count}` slots kept), `src/lib/copy/index.ts` (loader unchanged in shape — EN fallback per key), `src/lib/copy/copy.test.ts` (extend coverage: key parity, no-blank, count slots, no-inline-duplicate checks for newly covered surfaces), wiring across `src/routes/*/+page.svelte` + `src/lib/components/*` + `src/lib/*.ts` (replace hardcoded literals with `t()`/`tCount()`, incl. 216 bezel-Y/drag strings if any); ticket 220 ACs (extended) | Owns dictionary contract: `section.key`, EN source of truth, key-by-key EN fallback, identical EN/zh-CN key sets, noun titles + Discord-tone descriptions per ADR-0028 2026-09-17 amendment (spec-214 2nd-person rule superseded), 140-char hint cap, ticket-33 bans (no "Sprout never…", no wordplay, single "Loading…"), files-only (never DB). zh-CN MUST follow Discord-zh + Notion-zh consistency via research skill (web primary sources: Discord Chinese client/support, Notion Chinese help; record sources in return; e.g. 设置/主题/开机自启/停靠/快捷操作 naming, keep `Sprout`/presence `Using Sprout` handling as decided). No ADR text changes; no new Windows-invocation site; gate must pass. | Worker implements in `ticket-i18n`; owns copy/coverage tests + lightweight checks only. Heavy checks queued for coordinator. Must use research skill (or websearch primary sources) for zh-CN consistency and cite sources in return. | 48 code paths (r1) + r2 (3-file shadow stray-block preventive fix) + r3 (en/zh modelNameFirst ban fix + test path/key fixes); 1178/1178 keys; Discord-zh/Notion-zh sources cited in return; 216 strings covered (quickwindow.bezelPosition/bezelSaveFail) | validated |

## Integration order

1. Either return may reconcile first — expected disjoint except the planned overlap below.
2. Reconcile each return into `...\batch-217-216fix-220fix-20260917\staging` by applying only changed paths (three-way baseline/worker/staging where needed).
3. Planned shared-hotspot overlap (same file / different symbols — integration, not a behavioral dependency): `src/routes/quick-launch-window/+page.svelte` (217 bezel-interaction block vs i18n string wiring across same file). Coordinator owns the merge in staging; no silent last-writer-wins. Rule: 217 owns bezel tab structure/behavior symbols; i18n owns literal→`t()` replacements elsewhere in the file; bezel tooltip/peekHint lines are the seam — both must converge on `t("dock.bezel.tooltip")` / `t("dock.bezel.peekHint")`, never literals. `src/lib/copy/*` is i18n-only (217 read-only). `constants/window.rs` touched by neither (215 owns it).
4. Apply one reviewed candidate to `C:\Sprout` with pre-apply recovery, validate the combination, mark ACs only for validated tickets, run the ownership gate, publish with guarded `-Up`, verify second `-Up` is `0 copied`.

## Shared contracts (pinned before consumers start)

- Bezel geometry (215, frozen): collapsed 14px / peek 24px / height 12% clamped 64–160px / open cap 60% overlay / zero reservation collapsed+peek / centered Y default 0.5 (`BEZEL_Y_RATIO_DEFAULT`, `clamp_bezel_y_ratio`, `bezel_tab_y_px` in `constants/window.rs` — neither worker redefines).
- Bezel Y memory (216, frozen): `bezel_y_ratio 0..1` identity-key pattern; clamp on load; center fallback; side reuses edge memory.
- Copy keys: `section.key`; EN fallback per key; identical EN/zh-CN sets; noun titles + Discord-tone descriptions (ADR-0028 2026-09-17); 140-char hint cap + ticket-33 bans; files only.
- Presence: ADR-0033 allowlist unchanged; wording-only.
- Motion: ADR-0034 tokens `--dur-slow:280ms` + `--ease-spring`; easing for announcements, spring for interruptible arrivals; dock driver untouched.
- No second Windows-invocation site (ADR-0029); gate must pass.

## Candidate and recovery ledger

- Baseline manifest SHA-256: `A5E2716C33E56B9ABAA14B1C0D2566998641744B725DE9D51B3430F5B52C8C05` (573 files).
- Workspaces: worker `ticket-217`, worker `ticket-i18n`, reconciliation `staging`, all under `C:\Users\admin\AppData\Local\Temp\opencode\batch-217-216fix-220fix-20260917\`.
- Coordinator runs no ticket — both units in isolated workers; coordinator reconciles, validates, and publishes.
- Returns: `ticket-217` r1 + `ticket-i18n` r1 received (frozen); repairs `ticket-i18n-r2` (settings shadow, stray test block, Dialog preventive) + `ticket-i18n-r3` (modelNameFirst ban, chrome.close, ENOENT paths, moveToGroup) received and reconciled. Merge helper produced staging quick-launch page (SHA 57ef6f01…, 3699 lines).
- Candidate `candidate-217-i18n-r3` (50 code paths: 48 i18n + merged page + bezel test; +10 integration test files updated by coordinator per worker §6 +3 follow-up fixes) applied to `C:\Sprout`; pre-apply recovery at `recovery/before-apply` (50 files incl. recorded absence for bezelInteraction.test.ts). r2/r3 applied on top with same recovery (original baseline versions).
- Validation on the applied combination: `npm.cmd run check` 0 errors (3 pre-existing warnings — QuickActionFormDialog ×2, UnifiedHeader ×1); 15-file vitest 288/288 pass (copy 42 + bezelInteraction 7 + bezelY + windowChrome/settingsSearch/companion/etc.); full `npm.cmd exec vitest run` 421/422 — single failure `managedSettings.close.test.ts` "Available to install" is PRE-EXISTING 219 fallout (en.json has "Available models"; previous batch 216-220 recorded same; blocks nothing); `node tools/ownership-gate.mjs` pass (62 refs); `node tools/contrast-check.mjs` pass both themes.
- 217: validated. i18n-full (216fix+220fix): validated. 216 backend unchanged, bezelY tests pass, Y/drag strings covered (quickwindow.bezelPosition/bezelSaveFail).

## Conflict ledger

None at dispatch. Planned shared-hotspot file (`src/routes/quick-launch-window/+page.svelte`) has named symbols/intent above with coordinator-owned merge. If either return touches outside its claim (esp. `constants/window.rs`, ADRs, or `docs/CONTEXT.md`), stop and reconcile before staging.

## Conflict ledger — integration

No silent last-writer-wins. Overlapping file `src/routes/quick-launch-window/+page.svelte` merged by coordinator helper from VI18N base + V217 bezel block (13 hunks: imports, 164-line interaction block, grip/refreshDock/switchEdge/onMount/click-outside/Esc hooks, tab/panel template, CSS). Seam converged on t("dock.bezel.tooltip")/t("dock.bezel.peekHint"); VI18N wins for all string wirings (grip aria-label → t("quickwindow.bezelPosition"), persist error → t("quickwindow.bezelSaveFail"), SEAM_REASON inlined, all button/menu/placeholder literals → t()); V217 new error literal kept as-is flagged for i18n follow-up. All other overlapping test files (10) are coordinator-owned integration edits per worker §6 (literal → t() refs), plus 3 follow-up fixes (Back/Forward, Switching, paneHide/Restore). No ADRs, CONTEXT, constants, or backend touched. `managedSettings.close` failure is pre-existing 219 fallout, not a batch conflict.
